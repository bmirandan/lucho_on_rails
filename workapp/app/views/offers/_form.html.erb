<%= form_for(offer) do |f| %>
  <% if offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :position %>
    <%= f.text_field :position %>
  </div>

  <div class="field">
    <%= f.label :location %>
    <%= f.text_field :location %>
  </div>

  <div class="field">
    <%= f.label :startDate %>
    <%= f.datetime_select :startDate %>
  </div>

  <div class="field">
    <%= f.label :deadlineDate %>
    <%= f.datetime_select :deadlineDate %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
